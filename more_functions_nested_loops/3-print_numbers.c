#include <stdio.h>
/**
 * print_numbers - check the code
 * @a: variable
 * Return: result.
 */
void print_numbers(void a)
{
	char a;

	for (a = 48; a <= 57; a++)
	{
		_putchar(a);
	}
	_putchar('\n');
}
