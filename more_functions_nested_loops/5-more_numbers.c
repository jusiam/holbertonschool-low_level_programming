#include <stdio.h>
#include "main.h"

/**
 * more_numbers - check the code.
 *
 * Return: 0
 */

{
	void more_numbers(void);
    int x, y

	for (x = 0; x <= 9; x++)
	{
		for (y = 0; y <= 14; y++)
				_putchar(y)
		_putchar('\n');
	}
}
