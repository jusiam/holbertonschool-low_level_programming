#include "main.h"

/**
 * print_last_digit - print last digit of a numer n.
 * @: The integer to be computed.
 *
 * Return: value of the last digit.
 */
int print_last_digit(int n)
{
n = n % 10
	if (n >= 0)
		return (n);
	else
		return (-n);
}
