#include <stdio.h>
#include "main.h"
/**
 * main - check the code
 * @argc: argc
 * @argv: argv
 * Return: Always 0.
 */
int main(char *argv[])
{
	int i = 0;
	printf("%s\n", argv[i]);
	i++;
	return (0);
}
